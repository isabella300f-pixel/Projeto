# ============================================================
# KPI DASH 300 - Variáveis de Ambiente
# Copie para .env.local e preencha. No Vercel, use Settings > Environment Variables.
# ============================================================

# --- Supabase (obrigatório para o dashboard e sync) ---
# URL do projeto: https://ficdejjcoiwfogotisxv.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave anônima (anon public) - para leitura no cliente, se usar no futuro
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Chave service_role (NUNCA exponha no front-end) - usada pela API para ler/gravar no Supabase
SUPABASE_SERVICE_ROLE_KEY=

# --- Opcional: Sync Google Sheets → Supabase ---
# Se você usar POST /api/sync-sheets, a API chama /api/google-sheets. Em produção (Vercel) use:
# NEXT_PUBLIC_APP_URL=https://seu-app.vercel.app
# (O Vercel preenche VERCEL_URL automaticamente; use NEXT_PUBLIC_APP_URL só se precisar de URL absoluta.)
